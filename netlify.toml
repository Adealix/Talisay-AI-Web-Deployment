[build]
  command   = "npx expo export --platform web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — Expo Router needs all paths to serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
